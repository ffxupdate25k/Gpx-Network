import{api}from"./web-api.js";import{notify,haptic}from"./web-telegram.js";import{fail}from"./web-utils.js";import{bottom}from"./web-page-dashboard.js";
export default{async render(el){const me=await api.getMe();el.innerHTML=`<section class="page"><div class="top"><button class="page-back">‹</button><div class="top-copy"><h1>Withdrawal</h1></div></div><div class="body"><div class="walletbalance"><div><small>USDT Balance</small><strong>${Number(me.usdt_balance).toFixed(2)} USDT</strong><span>Withdrawals: max ${me.withdrawals_per_day}/day</span></div><div class="coin">₮</div></div><div class="card"><div class="row"><span>Minimum</span><b>${Number(me.min_withdraw).toFixed(2)} USDT (${(Number(me.min_withdraw)/0.0001).toLocaleString()} GPX)</b></div><div class="row"><span>Daily Limit</span><b>${me.withdrawals_per_day} times per day</b></div><div class="row"><span>Network</span><b>BEP20 (BNB Smart Chain)</b></div><label>Payout Address</label>${me.wallet_address?`<div class="walletaddr">${me.wallet_address}</div>`:`<input id="addr" placeholder="Enter your USDT BEP20 address"><button class="btn ghost" id="save">Save Payout Wallet</button>`}<label>Amount (USDT)</label><input id="amount" type="number" min="${me.min_withdraw}" step="0.01" placeholder="0.10"><button class="btn" id="submit">➤ Request Withdrawal</button><p class="hint">${me.auto_payout?'Payouts are sent automatically when configured.':'Payouts are reviewed by an admin.'}</p></div></div>${bottom("wallet")}</section>`;if(!me.wallet_address){el.querySelector("#save").onclick=async()=>{const a=el.querySelector("#addr").value.trim();try{await api.saveWallet(a);notify("Payout wallet saved.");this.render(el)}catch(e){fail(e)}}}el.querySelector("#submit").onclick=async()=>{const amount=Number(el.querySelector("#amount").value);if(!amount||amount<me.min_withdraw)return notify(`Minimum withdrawal is ${Number(me.min_withdraw).toFixed(2)} USDT.`);if(amount>me.usdt_balance)return notify("Amount is higher than your USDT balance.");try{await api.requestWithdrawal({amount});haptic("success");notify("Withdrawal requested. The bot will message you when it succeeds.");el.querySelector("#amount").value="";}catch(e){fail(e)}};}};
