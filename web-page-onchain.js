import{api}from"./web-api.js";import{notify,haptic,confirmBox}from"./web-telegram.js";import{fail}from"./web-utils.js";import{bottom}from"./web-page-dashboard.js";export default{async render(el){const me=await api.getMe();el.innerHTML=`<section class="page"><div class="top"><button class="page-back">‹</button><div class="top-copy"><h1>Onchain Transfer</h1><small>Mini App only</small></div></div><div class="body"><div class="tabs2"><button class="active" id="sendTab">Send</button><button id="receiveTab">Receive</button></div><div class="card"><b>Your GPX Wallet Address</b><div class="walletaddr">${me.gpx_wallet_address||"No wallet generated"}</div><div class="two"><button class="btn ghost" id="copy">Copy Address</button><button class="btn ghost" id="new">Generate New</button></div><button class="btn danger" id="revoke">Revoke Old Wallet</button></div><div class="card" id="sendBox"><b>Transfer GPX</b>${me.level<10?`<div class="locknote">🔒 You must be Level 10 to transfer to other users.<br>100 referrals = 1 level.</div>`:""}<label>Recipient Wallet Address</label><input id="to" placeholder="Enter wallet address"><label>Amount (GPX)</label><input id="amt" type="number" min="1" placeholder="Enter amount"><button class="btn" id="send" ${me.level<10?"disabled":""}>➤ Send GPX</button></div><div class="card" id="receiveBox" hidden><b>Receive GPX</b><p class="hint">Share this wallet address with another GPX Network user.</p><div class="walletaddr">${me.gpx_wallet_address||"No wallet generated"}</div><button class="btn ghost" id="copyReceive">Copy Address</button></div></div>${bottom("wallet")}</section>`;el.querySelector("#sendTab").onclick=()=>{el.querySelector("#sendTab").classList.add("active");el.querySelector("#receiveTab").classList.remove("active");el.querySelector("#sendBox").hidden=false;el.querySelector("#receiveBox").hidden=true};el.querySelector("#receiveTab").onclick=()=>{el.querySelector("#receiveTab").classList.add("active");el.querySelector("#sendTab").classList.remove("active");el.querySelector("#sendBox").hidden=true;el.querySelector("#receiveBox").hidden=false};el.querySelector("#copyReceive").onclick=async()=>{if(me.gpx_wallet_address){try{await navigator.clipboard.writeText(me.gpx_wallet_address);notify("Address copied.")}catch(_){notify(me.gpx_wallet_address)}}};el.querySelector("#copy").onclick=async()=>{if(me.gpx_wallet_address){try{await navigator.clipboard.writeText(me.gpx_wallet_address);notify("Address copied.")}catch(_){notify(me.gpx_wallet_address)}}};el.querySelector("#new").onclick=async()=>{if(!(await confirmBox("Generate a new wallet? Your old wallet will be revoked.")))return;try{const r=await api.generateGpxWallet();notify("New GPX wallet generated.");me.gpx_wallet_address=r.address;el.querySelector(".walletaddr").textContent=r.address}catch(e){fail(e)}};el.querySelector("#revoke").onclick=async()=>{if(!(await confirmBox("Revoke the old GPX wallet?")))return;try{await api.revokeGpxWallet();notify("Old wallet revoked. Generate a new wallet to receive transfers.");el.querySelector(".walletaddr").textContent="Revoked"}catch(e){fail(e)}};el.querySelector("#send").onclick=async()=>{try{const r=await api.transferGpx(el.querySelector("#to").value.trim(),Number(el.querySelector("#amt").value));haptic("success");notify(`Sent ${Number(r.amount).toLocaleString()} GPX to ${r.recipient}.`);el.querySelector("#amt").value="";}catch(e){fail(e)}};}};