import{api}from"./web-api.js";import{notify}from"./web-telegram.js";import{fail,esc}from"./web-utils.js";import{bottom}from"./web-page-dashboard.js";export default{async render(el,{go}){const [me,h]=await Promise.all([api.getMe(),api.getHistory()]);el.innerHTML=`<section class="page"><div class="top"><button class="page-back">‹</button><div class="top-copy"><h1>Wallet</h1></div></div><div class="body"><div class="walletbalance"><div><small>Total GPX Balance</small><strong>${Number(me.balance).toLocaleString()} GPX</strong><span>≈ $${(Number(me.balance)*0.0001).toFixed(2)} USDT</span></div><div class="coin">G</div></div><div class="wallet-actions"><button data-go="convert">${"↔"}<b>Convert</b></button><button data-go="withdrawal">↓<b>Withdraw</b></button><button data-go="onchain">↗<b>Transfer</b></button></div><div class="card"><div class="sectionhead"><b>Transactions</b><button class="linkbtn" id="view-history">View All ›</button></div>${h.slice(0,6).map(x=>`<div class="txrow"><span class="txicon">${x.amount>=0?"↗":"↘"}</span><div><b>${esc(x.title)}</b><small>${new Date(x.date).toLocaleString()}</small></div><strong class="${x.amount>=0?"plus":"minus"}">${x.type==='withdrawal'?(x.amount>=0?"+":"−")+Math.abs(Number(x.amount)).toFixed(2)+" USDT":(x.amount>=0?"+":"−")+Math.abs(Number(x.amount)).toLocaleString()+" GPX"}</strong></div>`).join("")||`<div class="empty">No transactions yet.</div>`}</div></div>${bottom("wallet")}</section>`;el.querySelectorAll("[data-go]").forEach(b=>b.onclick=()=>go(b.dataset.go));el.querySelector("#view-history").onclick=()=>go("history");}};