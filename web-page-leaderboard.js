import{api}from"./web-api.js";import{esc}from"./web-utils.js";import{bottom}from"./web-page-dashboard.js";export default{async render(el){const d=await api.getLeaderboard();el.innerHTML=`<section class="page"><div class="top"><button class="page-back">‹</button><div class="top-copy"><h1>Leaderboard</h1></div></div><div class="body"><div class="leader-tabs"><button class="on" id="top">Top Users</button><button id="refs">Referrals</button></div><div class="card leaderboard" id="list">${d.map((u,i)=>`<div class="leader"><span>${i+1}</span><div class="miniavatar">${esc((u.username||u.name||"U")[0].toUpperCase())}</div><b>${esc(u.username?"@"+u.username:u.name)}</b><strong class="score">${Number(u.balance).toLocaleString()} GPX</strong></div>`).join("")}</div></div>${bottom("invite")}</section>`;el.querySelector("#top").onclick=()=>{el.querySelector("#top").classList.add("on");el.querySelector("#refs").classList.remove("on");el.querySelectorAll(".score").forEach((x,i)=>x.textContent=Number(d[i].balance).toLocaleString()+" GPX")};el.querySelector("#refs").onclick=()=>{el.querySelector("#refs").classList.add("on");el.querySelector("#top").classList.remove("on");el.querySelectorAll(".score").forEach((x,i)=>x.textContent=Number(d[i].referrals).toLocaleString()+" refs")};}};