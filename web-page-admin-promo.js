import{api}from"./web-api.js";import{notify,haptic}from"./web-telegram.js";import{esc,fail}from"./web-utils.js";export default{async render(el){const codes=await api.admin.promoCodes();el.innerHTML=`<div class="card"><b>Create Promo Code</b><label>Code</label><input id="code" placeholder="GPXWELCOME"><label>Reward (GPX)</label><input id="reward" type="number" value="1000"><label>Maximum uses (0 = unlimited)</label><input id="max" type="number" value="0"><button class="btn" id="add">Create Code</button></div><div class="card"><b>Promo Codes</b>${codes.map(c=>`<div class="row"><span><b>${esc(c.code)}</b><small> +${Number(c.reward).toLocaleString()} GPX • ${c.uses}/${c.max_uses||"∞"}</small></span><button class="btn danger sm" data-del="${esc(c.code)}">Disable</button></div>`).join("")||`<div class="empty">No promo codes.</div>`}</div>`;el.querySelector("#add").onclick=async()=>{try{await api.admin.createPromo({code:el.querySelector("#code").value,reward:el.querySelector("#reward").value,max_uses:el.querySelector("#max").value});notify("Promo code created.");haptic("success");this.render(el)}catch(e){fail(e)}};el.querySelectorAll("[data-del]").forEach(b=>b.onclick=async()=>{try{await api.admin.deletePromo(b.dataset.del);notify("Promo disabled.");this.render(el)}catch(e){fail(e)}})}};