import{api}from"./web-api.js";import{notify,haptic}from"./web-telegram.js";import{fail}from"./web-utils.js";import{bottom}from"./web-page-dashboard.js";export default{async render(el){const me=await api.getMe();el.innerHTML=`<section class="page"><div class="top"><button class="page-back">‹</button><div class="top-copy"><h1>Convert to USDT</h1></div></div><div class="body"><div class="convertbox"><div class="coin">G</div><strong>1000</strong><span>→</span><strong>0.10 USDT</strong><div class="pair">GPX <small>1 GPX = 0.0001 USDT</small> USDT</div></div><div class="card"><b>Conversion Details</b><div class="row"><span>Rate</span><b>100 GPX = 0.01 USDT</b></div><div class="row"><span>Minimum</span><b>1,000 GPX (0.10 USDT)</b></div><div class="row"><span>Your Balance</span><b>${Number(me.balance).toLocaleString()} GPX</b></div><label for="conv">Amount (GPX)</label><input id="conv" type="number" min="1000" step="100" placeholder="1000"><button class="btn" id="do">↔ Convert Now</button></div><div class="info">ⓘ Converted USDT will be added to your wallet balance and can be withdrawn (2 withdrawals per day).</div></div>${bottom("wallet")}</section>`;el.querySelector("#do").onclick=async()=>{const amount=Number(el.querySelector("#conv").value);if(!Number.isFinite(amount)||amount<1000){return notify("Minimum conversion is 1,000 GPX.")}try{const r=await api.convert(amount);haptic("success");notify(`Converted ${amount.toLocaleString()} GPX to ${Number(r.usdt).toFixed(2)} USDT.`);el.querySelector("#conv").value="";}catch(e){fail(e)}};}};