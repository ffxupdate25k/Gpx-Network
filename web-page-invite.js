import{api}from"./web-api.js";import{openTelegramLink,haptic,notify}from"./web-telegram.js";import{esc,fail}from"./web-utils.js";import{icons}from"./web-icons.js";import{bottom}from"./web-page-dashboard.js";export default{async render(el){const [me,d]=await Promise.all([api.getMe(),api.getReferrals()]);el.innerHTML=`<section class="page"><div class="top"><button class="page-back">‹</button><div class="top-copy"><h1>Invite Friends</h1></div></div><div class="body"><div class="feature"><div class="feature-art">${icons.invite}</div><h2>Invite Friends</h2><p>Get more GPX when your friends join and complete tasks!</p></div><div class="card rewardcard"><div class="ic">🎁</div><div><b>Referral Reward</b><strong>+${d.reward} GPX</strong><small>per successful invite</small></div></div><div class="card"><small>Your Referral Link</small><div class="link">${esc(me.referral_link)} <button id="copy">${icons.copy}</button></div><button class="btn" id="share">➤ Share Link</button></div><div class="stat"><div class="card"><b>${d.count}</b><small>Referrals</small></div><div class="card"><b>${Number(d.earned).toLocaleString()} GPX</b><small>Total Earned</small></div></div><div class="card"><b>Referral Reward Milestones</b>${d.milestones.map(m=>`<div class="milestone"><span>✓</span><div>${m.referrals.toLocaleString()} Referrals → Level ${m.level}<small>${m.reward?`+${m.reward.toLocaleString()} GPX reward`:""}</small></div><em>${Math.min(d.count,m.referrals)}/${m.referrals}</em></div>`).join("")}</div></div>${bottom("invite")}</section>`;el.querySelector("#share").onclick=()=>openTelegramLink("https://t.me/share/url?url="+encodeURIComponent(me.referral_link)+"&text="+encodeURIComponent("Join me on GPX Network and earn GPX!"));el.querySelector("#copy").onclick=async()=>{try{await navigator.clipboard.writeText(me.referral_link);haptic("success");notify("Referral link copied.")}catch(_){notify(me.referral_link)}};}};