import{api}from"./web-api.js";import{notify,haptic}from"./web-telegram.js";import{fail}from"./web-utils.js";import{bottom}from"./web-page-dashboard.js";import{emitActivity}from"./web-live.js";export default{async render(el){const codes=await api.getPromoCodes();el.innerHTML=`<section class="page"><div class="top"><button class="page-back">‹</button><div class="top-copy"><h1>Promo Code</h1></div></div><div class="body"><div class="feature promo"><div class="feature-art">🎁</div><h2>Enter Promo Code</h2><p>Get free GPX with special promo codes!</p></div><div class="card"><input id="code" placeholder="Enter code here..."><button class="btn" id="redeem">🎁 Redeem</button></div><div class="card"><b>Available Codes</b>${codes.map(c=>`<div class="code"><span>${c.code}</span><strong>+${Number(c.reward).toLocaleString()} GPX</strong></div>`).join("")||`<div class="empty">No active codes.</div>`}</div></div>${bottom("home")}</section>`;el.querySelector("#redeem").onclick=async()=>{try{const r=await api.redeemPromo(el.querySelector("#code").value.trim());haptic("success");notify(`Promo redeemed: +${Number(r.reward).toLocaleString()} GPX.`);emitActivity({type:"promo",reward:r.reward});el.querySelector("#code").value="";}catch(e){fail(e)}};}};